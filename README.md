# Windows

## Setup

Enable scripts `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

Edit Winget dependencies in `install.ps1`

Run `./install.ps1`

## Colors

[Catppuccin for Windows Terminal](https://github.com/catppuccin/windows-terminal)

# Linux

Run `./install.sh`